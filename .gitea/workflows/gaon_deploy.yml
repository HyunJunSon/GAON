name: Deploy GAON Application

on:
  push:
    branches: [ "main", "develop", "hotfix/fix-code" ]

env:
  REGISTRY: asia-northeast3-docker.pkg.dev
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REPOSITORY: gaon-docker-hub
  BACKEND_IMAGE: backend
  FRONTEND_IMAGE: frontend

jobs:
  check-changes:
    runs-on: self-hosted
    outputs:
      backend_changed: ${{ steps.changes.outputs.backend_changed }}
      frontend_changed: ${{ steps.changes.outputs.frontend_changed }}
      migration_changed: ${{ steps.changes.outputs.migration_changed }}
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # 전체 히스토리 가져오기
    
    - name: Check changed files
      id: changes
      run: |
        # 최근 2개 커밋에서 변경된 파일들 확인 (머지 커밋 대응)
        # HEAD~2가 없는 경우 전체 파일을 변경된 것으로 처리
        if git rev-parse --verify HEAD~2 >/dev/null 2>&1; then
          CHANGED_FILES=$(git diff --name-only HEAD~2 HEAD)
        else
          echo "HEAD~2 not available, treating all files as changed"
          CHANGED_FILES=$(find . -name "*.py" -o -name "*.js" -o -name "*.ts" -o -name "*.tsx" -o -name "*.json" -o -name "Dockerfile*" | grep -E "(backend/|frontend/)" | head -10)
        fi
        
        echo "backend_changed=$(echo "$CHANGED_FILES" | grep -q '^backend/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "frontend_changed=$(echo "$CHANGED_FILES" | grep -q '^frontend/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        echo "migration_changed=$(echo "$CHANGED_FILES" | grep -q '^backend/alembic/versions/' && echo 'true' || echo 'false')" >> $GITHUB_OUTPUT
        
        # 디버깅용 로그
        echo "Changed files: $CHANGED_FILES"

  build-backend:
    needs: check-changes
    if: needs.check-changes.outputs.backend_changed == 'true'
    runs-on: self-hosted
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        path: gaon-source

    - name: Authenticate to Google Cloud
      run: |
        export PATH=$PATH:/home/ubuntu/google-cloud-sdk/bin
        echo '${{ secrets.GCP_SA_KEY }}' > /tmp/gcp-key.json
        gcloud auth activate-service-account --key-file=/tmp/gcp-key.json
        gcloud auth configure-docker ${{ env.REGISTRY }}
        rm /tmp/gcp-key.json

    - name: Build and push backend image
      run: |
        export PATH=$PATH:/home/ubuntu/google-cloud-sdk/bin
        IMAGE_TAG=${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.BACKEND_IMAGE }}
        docker build -t ${IMAGE_TAG}:${{ github.sha }} -t ${IMAGE_TAG}:latest -t gaon:back-server ./gaon-source/backend
        docker push ${IMAGE_TAG}:${{ github.sha }}
        docker push ${IMAGE_TAG}:latest

  build-frontend:
    needs: check-changes
    if: needs.check-changes.outputs.frontend_changed == 'true'
    runs-on: self-hosted
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        path: gaon-source

    - name: Authenticate to Google Cloud
      run: |
        export PATH=$PATH:/home/ubuntu/google-cloud-sdk/bin
        echo '${{ secrets.GCP_SA_KEY }}' > /tmp/gcp-key.json
        gcloud auth activate-service-account --key-file=/tmp/gcp-key.json
        gcloud auth configure-docker ${{ env.REGISTRY }}
        rm /tmp/gcp-key.json

    - name: Build and push frontend image
      run: |
        export PATH=$PATH:/home/ubuntu/google-cloud-sdk/bin
        IMAGE_TAG=${{ env.REGISTRY }}/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.FRONTEND_IMAGE }}
        docker build -t ${IMAGE_TAG}:${{ github.sha }} -t ${IMAGE_TAG}:latest -t gaon:front-server ./gaon-source/frontend
        docker push ${IMAGE_TAG}:${{ github.sha }}
        docker push ${IMAGE_TAG}:latest

  deploy:
    needs: [check-changes, build-backend, build-frontend]
    if: always() && (needs.build-backend.result == 'success' || needs.build-backend.result == 'skipped') && (needs.build-frontend.result == 'success' || needs.build-frontend.result == 'skipped') && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/hotfix/fix-code')
    runs-on: self-hosted
    
    steps:
    - name: Deploy to OCI Server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.OCI_HOST }}
        username: ${{ secrets.OCI_USERNAME }}
        key: ${{ secrets.OCI_SSH_KEY }}
        port: ${{ secrets.OCI_PORT }}
        script: |
          set -e
          
          # 환경변수 설정
          export REGISTRY="${{ env.REGISTRY }}"
          export PROJECT_ID="${{ env.PROJECT_ID }}"
          export REPOSITORY="${{ env.REPOSITORY }}"
          
          cd /home/ubuntu/gaon
          
          # 로그 디렉토리 권한 설정
          sudo mkdir -p logs
          sudo chown -R 1000:1000 logs
          
          # 최신 이미지 pull (Docker 인증은 이미 되어 있음)
          docker pull ${REGISTRY}/${PROJECT_ID}/${REPOSITORY}/backend:latest || echo "Backend 이미지 pull 실패, 로컬 이미지 사용"
          docker pull ${REGISTRY}/${PROJECT_ID}/${REPOSITORY}/frontend:latest || echo "Frontend 이미지 pull 실패, 로컬 이미지 사용"
          
          # 간단한 이름으로 태그 생성
          docker tag ${REGISTRY}/${PROJECT_ID}/${REPOSITORY}/backend:latest gaon:back-server
          docker tag ${REGISTRY}/${PROJECT_ID}/${REPOSITORY}/frontend:latest gaon:front-server
          
          # 환경 변수 설정
          export BACKEND_IMAGE=gaon:back-server
          export FRONTEND_IMAGE=gaon:front-server
          
          # 현재 실행 중인 이미지 백업 (롤백용)
          CURRENT_BACKEND=$(docker inspect gaon-backend --format='{{.Config.Image}}' 2>/dev/null || echo "")
          CURRENT_FRONTEND=$(docker inspect gaon-frontend --format='{{.Config.Image}}' 2>/dev/null || echo "")
          
          # 롤백 함수
          rollback() {
            echo "🚨 Error detected! Rolling back..."
            if [ ! -z "$CURRENT_BACKEND" ] && [ ! -z "$CURRENT_FRONTEND" ]; then
              export BACKEND_IMAGE=$CURRENT_BACKEND
              export FRONTEND_IMAGE=$CURRENT_FRONTEND
              docker-compose -f docker-compose.prod.yml up -d --no-deps gaon-backend
              docker-compose -f docker-compose.prod.yml up -d --no-deps gaon-frontend
              docker-compose -f docker-compose.prod.yml up -d --no-deps gaon-nginx
              echo "✅ Rollback completed!"
            fi
            exit 1
          }
          
          # 에러 시 롤백 실행
          trap rollback ERR
          
          # 롤링 업데이트 (무중단 배포)
          echo "🚀 Starting rolling update..."
          
          # 기존 컨테이너 강제 정리 (컨테이너 이름 충돌 방지)
          echo "🧹 Cleaning up existing containers..."
          docker rm -f gaon-backend gaon-frontend gaon-nginx gaon-postgres 2>/dev/null || true
          docker rm -f $(docker ps -aq --filter "name=gaon") 2>/dev/null || true
          docker-compose -f docker-compose.prod.yml down --remove-orphans || true
          docker system prune -f || true
          
          echo "⏳ Waiting for cleanup to complete..."
          sleep 5
          
          # 새로 시작
          docker-compose -f docker-compose.prod.yml up -d || rollback
          echo "⏳ Waiting for services to start..."
          sleep 20
          
          # DB 마이그레이션 적용 (마이그레이션 변경사항이 있을 때)
          if [ "${{ needs.check-changes.outputs.migration_changed }}" == "true" ]; then
            echo "🔄 Applying database migrations..."
            docker exec -w /app gaon-backend alembic upgrade head || rollback
            echo "✅ Database migrations applied successfully!"
          fi
          
          # 헬스체크
          if ! curl -f http://localhost:8000/health; then
            echo "❌ Backend health check failed!"
            rollback
          fi
          
          # 정리
          docker image prune -f
          
          echo "✅ Deployment completed successfully!"
